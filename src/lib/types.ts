export interface Prize {
    id: string
    name: string
    count: number
    color: string
  }
  
  export interface SpinRecord {
    id: string
    prizeName: string
    timestamp: string
    prizeColor: string
  }
  